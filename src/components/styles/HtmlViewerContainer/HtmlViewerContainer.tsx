import styled from 'styled-components';

const HtmlViewerContainer = styled.div`
  * {
    word-break: break-word;
  }   
  img, table {
    max-width: 100% !important;
  }
  
`;

export default HtmlViewerContainer;
